#!/bin/bash

# Часть 4. Генерация тестовых логов nginx за пять дней.
#
# Логи нужны частям 5 и 6: разбирать реальные логи веб-сервера негде,
# поэтому они генерируются в формате COMBINED — том же, что nginx пишет
# по умолчанию, и который понимает goaccess.
# main.sh — основной управляющий скрипт генерации логов nginx

# Генерация логов за 5 дней
for i in {0..4}; do
  source "$(dirname "$0")/generate_log.sh" "$i"
done 