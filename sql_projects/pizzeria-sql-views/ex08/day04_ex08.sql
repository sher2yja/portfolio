-- Уборка: удаляются все шесть объектов, созданных за день.
--
-- Материализованное представление требует отдельной команды: DROP VIEW на нём
-- прерывается с «is not a view» и подсказкой использовать DROP MATERIALIZED
-- VIEW. Для сервера это разные виды объектов — снимок хранит данные и занимает
-- место, обычное представление не хранит ничего.
--
-- Порядок здесь обратный созданию, но зависимостями он не продиктован:
-- ни одно представление дня не построено поверх другого, и удалять их можно
-- в любой последовательности. Обратный порядок выбран как привычка: он
-- остаётся верным и тогда, когда зависимости появятся.
DROP MATERIALIZED VIEW mv_dmitriy_visits_and_eats;

DROP VIEW v_price_with_discount;

DROP VIEW v_symmetric_union;

DROP VIEW v_generated_dates;

DROP VIEW v_persons_male;

DROP VIEW v_persons_female;
